const test = QUnit.test;

QUnit.module('Render');

test('renders', assert => {


    // act
    const actual = true;
    const expected = true;
    // assert
    assert.deepEqual(actual, expected);
});

